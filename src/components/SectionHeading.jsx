const SectionHeading = ({ text }) => {
  return (
    <button className="px-5 py-2.5 w-32 uppercase rounded-md bg-app-blue text-white font-semibold text-sm">
      {text}
    </button>
  );
};

export default SectionHeading;
